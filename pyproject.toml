[project]
name = "financial-document-intelligence-rag"
version = "0.2.0"
description = "Financial document intelligence RAG with real ingestion, indexing, evaluation, and API."
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 120
